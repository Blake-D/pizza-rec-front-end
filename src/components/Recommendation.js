import React from 'react'

function Recommendation(props){
    return(
        <div>
            {/* <ul>
                {props.pizzarias.map(place => (
                    <li>
                        <button>{place.name}</button>
                    </li>
                ))}
            </ul> */}
        </div>
    )
}

export default Recommendation